package main

import (
	"errors"
)

// Template file

func day0(part int, data []string) (int, error) {
	switch part {
	case 1:
		return 0, nil
	case 2:
		return 0, nil
	default:
		return 0, errors.New("not implemented")
	}
}
